// トレース突合用のサンプルテスト（実行はしない — grep 対象としてのみ使う）
describe('order', () => {
  test('B-1: 在庫のある商品を注文できる', () => {})

  // 無効化されたテスト — スイートが緑でも実行されない（偽 ✅ の источник）
  test.skip('B-2: 在庫ゼロは拒否される', () => {})

  // spec に存在しない B-ID（typo か spec の陳腐化）
  test('B-9: 存在しない仕様への参照', () => {})

  // 取り消し線つき（削除済み）B 行への参照
  test('B-4: 旧仕様の割引表示', () => {})

  // 数値開始でないトークンは参照として扱わない（フィクスチャ文字列の誤検出防止）
  const fixtureName = 'B-ORDER'
})
